views: 1
action_dim: 2

workspace:
  _target_: workspaces.cube.CubeWorkspace

dataset:
  _target_: datasets.cube.CubeDataset
  data_directory: ${env_vars.datasets.cube}
  subset_fraction: ${subset_fraction}
  prefetch: True
  env_type: 'double'
